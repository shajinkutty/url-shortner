<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>URL Shortner</title>
    <link rel="stylesheet" href="style.css" />
  </head>
  <body>
    <% if (locals.error) { %>

    <h1><%= error %></h1>
    <% } %>

    <div class="container">
      <h2>Tiny URL</h2>
      <form action="/" method="post">
        <input type="url" id="url" name="url" placeholder="url here.." />
        <input type="submit" value="Create Url" />
      </form>
      <div class="output">
        <% if (locals.short_url) { %>
        <a href="<%= short_url %>" target="_blank"><%= output %> </a>
        <% } %>
      </div>
    </div>
  </body>
</html>
